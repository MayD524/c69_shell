func test {
    check-is equal $USER my -> tmp
    check-is equal $USER may
    if $tmp
        echo hello
    else
        echo goodbye
    end
    return 0
}

set-alias t call test
echo hello